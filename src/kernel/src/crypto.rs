// use p256::ecdsa::{signature::Signer, Signature, SigningKey};
// use sha2::{Digest, Sha256, Sha512};

// pub fn sha256(input: impl AsRef<[u8]>) {
//     let mut hasher = Sha256::new();
//     hasher.update(input);
//     let res = hasher.finalize();
// }
